@REM Virtual environment activation. You must have virtualenv installed.
@REM If you don't have it, delete the next line and just run the last one.
@REM Of course, you must have requirements.txt installed.
@REM use "pip install -r requirements.txt" to install it.
call .venv/Scripts/activate.bat
python -m main